// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
import { getFirestore, initializeFirestore, persistentLocalCache, persistentMultipleTabManager } from "firebase/firestore";
import { getDatabase, ref, onValue } from "firebase/database";
import { getAuth, } from "firebase/auth";
import { getFunctions } from "firebase/functions";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyBXQFjg_OQr6lEqXQ453Un_ZHBqXpA2uCg",
  authDomain: "breakpoint-ciledug.firebaseapp.com",
  databaseURL: "https://breakpoint-ciledug-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "breakpoint-ciledug",
  storageBucket: "breakpoint-ciledug.appspot.com",
  messagingSenderId: "474470382364",
  appId: "1:474470382364:web:e25997ac70bd6d1cc098d9",
  measurementId: "G-XXGMK6XC7H"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
initializeFirestore(app, 
  {localCache: 
    persistentLocalCache(/*settings*/{tabManager: persistentMultipleTabManager()})
});
const firestore = getFirestore(app);
const auth = getAuth(app);
const functions = getFunctions(app, 'asia-southeast1');
const db = getDatabase(app)
const analytics = getAnalytics(app);

export { app, auth, firestore, db , functions};